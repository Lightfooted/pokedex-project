<div class="card" style="width: 18rem;">
        <img class="card-img-top" src="{{front_default}}" alt="Card image cap">{{user.username}}
        <div class="card-body">{{id}} {{name}}
          <p class="card-text">{{{height}}} {{weight}} {{entry_number}}</p>

